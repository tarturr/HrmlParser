#ifndef HRMLPARSER_UTILS_HPP
#define HRMLPARSER_UTILS_HPP


#include <vector>
#include <string>
#include <array>

namespace Utils
{
    std::vector<std::string> scan(int lines_count);
}

#include "Utils.tpp"

#endif //HRMLPARSER_UTILS_HPP